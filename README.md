# homebrew-tap

Homebrew formulae for noctaIO projects.

```bash
brew tap noctaIO/tap
brew install obscene-spinner
```

Then `spin` (or `obscene-spinner`) is on your PATH. See
[obscene-spinner](https://github.com/noctaIO/obscene-spinner) for what it does.
